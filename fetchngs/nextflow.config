
manifest {
    description     = 'Pipeline to fetch metadata and raw FastQ files from public databases'
    nextflowVersion = '!>=25.10.0'
}

workflow.output.mode = 'copy'

profiles {
    test {
        params.input = "${projectDir}/data/input.json"
    }
}
